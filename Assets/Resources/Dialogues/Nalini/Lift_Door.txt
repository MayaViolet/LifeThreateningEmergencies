label start:
  if kettle_to_farm:
    transition: "Farm-Nal"
  endif

  if observed_kettle:
    transition: "Mess-Nal"
  endif
  
  if spoke_to_jordyn:
    transition: "Mess-Nal"
  endif
  
  if all_hell_broke_loose:
    transition: "Farm-Nal"
   endif
   
  if visited_farm_once:
    transition: "Mess-Nal"
  endif

  transition: "Farm-Nal"